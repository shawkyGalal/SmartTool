
<%
	java.util.concurrent.TimeUnit.SECONDS.sleep(1);
	 
%>

<%
    // Set the HTTP response status code (e.g., 200 for OK, 404 for Not Found, 500 for Internal Server Error)
    //response.setStatus(504); // Set to the desired status code

    // You can also set other response headers if needed
    // response.addHeader("Content-Type", "text/html; charset=UTF-8");
   
%>


{
	"abc": "123"
}